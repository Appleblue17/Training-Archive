# 2026“钉耙编程”中国大学生算法设计暑期联赛（10）复盘报告

## 一、总体表现总结

### 1.1 最终成绩概览

| 题目 | 状态 | 首次提交时间 | AC 时间 | 错误次数 | 罚时（含错误惩罚） |
|------|------|-------------|---------|----------|-------------------|
| K. 奶蛙的奶糖 | ✅ AC | 12:11:23 | 12:11:23 | 0 | 12 |
| H. 恋恋的排列 | ✅ AC | 12:20:37 | 12:20:37 | 0 | 21 |
| B. 大户爱的硬币 | ✅ AC | 12:27:36 | 12:27:36 | 0 | 28 |
| F. 恋恋的序列 | ✅ AC | 12:42:47 | 12:42:47 | 1（RE） | 63 |
| C. 大户爱的宿舍 | ✅ AC | 12:56:33 | 12:56:33 | 0 | 57 |
| J. 歪歪朋友圈 | ✅ AC | 13:08:41 | 13:08:41 | 2（RE/TLE） | 109 |
| D. 大户爱的干草堆 | ✅ AC | 13:27:53 | 13:27:53 | 0 | 88 |
| G. 小白的烦恼 | ✅ AC | 14:46:10 | 14:52:32 | 3（TLE×2/WA） | 172 |
| E. 大户爱的生成树2 | ✅ AC | 15:54:54 | 15:54:54 | 0 | 155 |
| I. 大改革 | ✅ AC | 15:27:17 | 16:07:08 | 1（WA）+ 6（CE） | 227 |
| A. 串串 | ❌ 未通过 | - | - | 0 | - |
| L. 飞翔的甲鱼 | ❌ 未通过 | - | - | 0 | - |

**最终结果：10/12 题通过，总罚时约 932 分钟。**

### 1.2 解题时间线

```
12:11  K AC（签到题，快速拿下）
12:20  H AC（前缀和/DP 思维题）
12:27  B AC（概率 + NTT 数学题）
12:42  F AC（线段树，一次 RE 后修复）
12:56  C AC（最大流 + 二分）
13:08  J AC（LIS 变形，两次错误后修复）
13:27  D AC（数论 + 莫队/离线）
14:46  G 首次提交（TLE）
14:52  G AC（分块 + 莫队优化）
15:27  I 首次提交（WA）
15:54  E AC（全局最小割 + 最小生成树）
16:07  I AC（贪心修正）
```

### 1.3 整体评价

**做得好的地方：**

1. **开局节奏极佳**：前 30 分钟内连续 AC 三题（K、H、B），迅速建立了罚时优势。K 题作为签到题在 12:11 即通过，为整场比赛奠定了良好基调。
2. **中期稳定输出**：12:42 至 13:27 之间连续 AC 四题（F、C、J、D），保持了稳定的解题节奏，没有出现长时间卡题的情况。
3. **难题攻坚能力**：E 题（全局最小割）和 I 题（贪心构造）属于中高难度题目，最终均成功解决，展现了队伍在复杂算法和思维题上的实力。
4. **封榜后仍有产出**：15:54 和 16:07 分别 AC 了 E 和 I，说明队伍在比赛后期依然保持了良好的竞技状态。

**不足之处：**

1. **F 题低级错误**：线段树合并操作中 `z.c[0]` 被赋值两次，导致 RE。这类笔误完全可以通过更仔细的代码审查避免。
2. **J 题调试时间过长**：从 12:46 到 13:08 共 22 分钟，经历了 RE 和 TLE 两次失败。RE 是因为调试输出未删除，TLE 是因为树状数组更新逻辑错误（`change` 函数中应为 `max` 而非直接赋值）。
3. **G 题多次 TLE**：分块 + 莫队的实现经历了三次 TLE 才通过，说明对复杂度的估算和常数优化不够敏感。
4. **I 题大量 CE**：从 16:20 到 16:45 出现了 6 次 CE，虽然这些 CE 可能是队员将提交当作"虚拟打印"使用（提交了无关的调试输出），但也反映出代码管理上的混乱。
5. **A 题和 L 题未能解决**：A 题（k-优秀子串）和 L 题（博弈）均未通过，说明队伍在字符串高级算法和博弈论方面仍有提升空间。

### 1.4 罚时分析

总罚时约 932 分钟，其中错误提交产生的额外罚时约为：
- F 题：1 次错误 × 20 = 20 分钟
- J 题：2 次错误 × 20 = 40 分钟
- G 题：3 次错误 × 20 = 60 分钟
- I 题：1 次错误 × 20 = 20 分钟（CE 不计罚时）

**额外罚时总计约 140 分钟**，这是一个不小的数字。如果减少这些错误，排名会有显著提升。

---

## 二、逐题分析

### K. 奶蛙的奶糖（签到题）

- **知识点**：暴力枚举、预处理、二分查找
- **难度**：★☆☆☆☆（签到题）
- **首次提交即 AC**，用时 12 分钟

**分析**：

本题要求找到不小于 x 的最小"立方奶糖编号" N，满足 popcount(N)³ = N >> 1，且 N ≤ 10⁹。

代码思路非常直接：预处理 1 到 2×10⁵ 范围内所有满足条件的数，然后对每个询问在预处理数组中二分查找（代码中使用了 `nxt` 数组记录每个位置之后第一个满足条件的数）。

```cpp
for(int i=1;i<N;i++){
    int x = pop_count(i);
    if(x*x*x == i>>1){
        ans[i] = 1;
    }
}
nxt[N-1] = -1;
for(int i=N-2;i>=1;i--){
    nxt[i] = nxt[i+1];
    if(ans[i]) nxt[i] = i;
}
```

**评价**：作为签到题，队伍在 12:11 就完成了 AC，节奏很好。代码简洁高效，没有多余操作。唯一的小问题是 `pop_count` 函数可以改用内置的 `__builtin_popcount`，但影响不大。

---

### H. 恋恋的排列（思维题）

- **知识点**：前缀和、后缀和、动态规划
- **难度**：★★☆☆☆
- **首次提交即 AC**，用时 20 分钟

**分析**：

本题要求翻转至多一个连续段后，最大化前缀和的最大值。

代码思路：枚举翻转段的右端点 i，维护前缀最大值 `p[i-1]` 和翻转段后缀和的最小前缀 `mn`，则答案为 `max(p[i-1] + s[i] - mn)`。

```cpp
int mn=0;
s[n+1]=0;
for(int i=n; i>=1; --i){
    s[i]=s[i+1]+a[i];
    ans=max(ans, p[i-1]+s[i]-mn);
    mn=min(mn, s[i]);
}
```

**评价**：这是一道典型的"翻转区间"思维题，核心在于将翻转操作转化为"前缀 + 后缀"的组合。队伍在 20 分钟内 AC，说明对这类题型的掌握比较熟练。代码简洁，逻辑清晰。

---

### B. 大户爱的硬币（数学 + NTT）

- **知识点**：概率论、生成函数、NTT（快速数论变换）、斯特林数
- **难度**：★★★★☆
- **首次提交即 AC**，用时 27 分钟

**分析**：

本题要求计算 Var[X^k]，其中 X 是几何分布（首次成功所需的试验次数）。

代码使用了生成函数 + NTT 的方法。核心思路是：
1. 计算 E[X^k] 和 E[X^(2k)]
2. 利用公式 Var[X^k] = E[X^(2k)] - (E[X^k])²

具体实现中，使用了斯特林数展开：
- E[X^k] = Σ S(k,i) × i! × (1-p)^i / p^(i+1)

代码中通过 NTT 快速计算斯特林数相关的卷积：

```cpp
for(int i=0;i<=k;i++) F[i]=ksm(i,k)%mod*inv[i]%mod;
for(int i=0;i<=k;i++) G[i]=ID(i)*inv[i]%mod;
NTT::solve(H,F,G,k,k);
```

**评价**：这是一道中等偏难的数学题，需要较强的生成函数和 NTT 功底。队伍在 27 分钟内 AC，说明数学基础扎实。NTT 模板实现完整，包括预处理、正变换、逆变换等，代码质量较高。

---

### F. 恋恋的序列（线段树）

- **知识点**：线段树、懒标记、区间赋值/取反/查询
- **难度**：★★★☆☆
- **AC 时间**：12:42，1 次 RE

**分析**：

本题要求支持区间赋值、区间取反、区间查询交替次数三种操作。

代码使用线段树，每个节点维护：
- `lc`、`rc`：区间左右端点的值
- `c[0]`、`c[1]`：区间内 0 和 1 的交替次数（考虑取反懒标记）

合并操作：

```cpp
info operator +(info x, info y){
    info z;
    z.lc=x.lc; z.rc=y.rc;
    z.c[0]=x.c[0]+y.c[0];
    z.c[1]=x.c[1]+y.c[1];
    z.c[0]+=x.rc^y.lc;  // 这里被重复赋值了！
    z.c[1]+=x.rc^y.lc;
    return z;
};
```

**错误分析**：第一次提交 RE 的原因是在 `operator+` 中，`z.c[0]` 被赋值了两次——先被赋值为 `x.c[0]+y.c[0]`，然后又被覆盖为 `x.rc^y.lc`。这导致线段树节点信息错误，最终引发数组越界或递归异常。

修复后的正确代码：

```cpp
z.c[0]=x.c[0]+y.c[0] + (x.rc^y.lc);
z.c[1]=x.c[1]+y.c[1] + (x.rc^y.lc);
```

**评价**：这是一道标准的线段树模板题，难度适中。RE 的原因是低级笔误，完全可以通过更仔细的代码审查避免。建议在写线段树合并操作时，先明确每个字段的含义，再逐行赋值。

---

### C. 大户爱的宿舍（最大流）

- **知识点**：最大流（Dinic）、二分答案
- **难度**：★★★☆☆
- **首次提交即 AC**，用时 56 分钟

**分析**：

本题要求最大化启用的宿舍数，每间宿舍需要恰好 K 名新生且每人分配到可接受的床位。

建模思路：
- 源点 → 每个新生（容量 1）
- 每个新生 → 可接受的床位（容量 1）
- 每个床位 → 汇点（容量为当前枚举的宿舍数）

代码采用逐步增加宿舍数的方式，每次将床位到汇点的容量 +1，然后跑 Dinic 判断是否满流：

```cpp
for(int ans=0;;ans++){
    for(int j=1;j<=k;j++) e[sav[j]].f++;
    int res=dinic();
    if(res<k){
        printf("%d\n",ans);
        break;
    }
}
```

**评价**：这是一道经典的最大流建模题。队伍在 56 分钟内 AC，说明对网络流建模比较熟练。代码中使用了 Dinic 算法，并利用残量网络的性质逐步增加容量，避免了重复建图，效率较高。

---

### J. 歪歪朋友圈（LIS 变形）

- **知识点**：最长上升子序列（LIS）、树状数组、置换
- **难度**：★★★☆☆
- **AC 时间**：13:08，2 次错误（RE/TLE）

**分析**：

本题要求计算从初始矩阵 A 到目标矩阵 B 最少需要拖动多少张照片。

核心观察：将 B 中每个元素映射到它在 A 中的位置，问题转化为求这个排列的最长上升子序列（LIS）。答案 = 总元素数 - LIS 长度。

```cpp
for(int i=1;i<=n*m;i++){
    f[i] = query(b[i])+1;
    change(b[i],f[i]);
    ans = max(ans,f[i]);
}
printf("%lld\n",n*m-ans);
```

**错误分析**：

1. **第一次 RE**（12:46）：代码中包含了调试输出语句（`printf`），且数组大小可能不够。这是典型的"忘记删除调试代码"的低级错误。

2. **第二次 TLE**（13:04）：树状数组的 `change` 函数写错了：

```cpp
void change(int x,int y){
    for(;x<N;x+=lowbit(x)){
        bit[x] = y;  // 错误！应该是 max(bit[x], y)
    }
}
```

这里应该是 `bit[x] = max(bit[x], y)`，因为 LIS 需要维护前缀最大值。直接赋值会导致信息丢失。

**评价**：本题的核心是识别出"最少拖动次数 = 总元素数 - LIS 长度"这一关键转化。队伍在 22 分钟内解决了这个问题，但两次错误提交暴露了代码审查不仔细的问题。

---

### D. 大户爱的干草堆（数论 + 离线）

- **知识点**：莫比乌斯反演、约数枚举、离线处理
- **难度**：★★★★☆
- **首次提交即 AC**，用时 87 分钟

**分析**：

本题要求对每个询问求 Σ gcd²(a_j, k)，其中 j 从 l 到 r。

代码思路：
1. 预处理莫比乌斯函数和所有数的约数
2. 利用公式 gcd²(a, k) = Σ_{d|gcd(a,k)} f(d)，其中 f(d) = Σ_{e|d} e² × μ(d/e)
3. 对每个 d，预处理所有 a_i 中能被 d 整除的位置
4. 对每个询问，枚举 k 的约数 d，用二分查找统计 [l, r] 中能被 d 整除的 a_i 个数

```cpp
for(int i=1;i<=lim;i++)
    for(int j=i;j<=lim;j+=i){
        f[j]+=i*i*mu[j/i];
    }
```

**评价**：这是一道中等偏难的数论题，需要莫比乌斯反演和约数枚举的技巧。队伍在 87 分钟内 AC，说明数论基础扎实。代码中使用了快速读入（`rd()` 函数），体现了对大数据量输入的性能意识。

---

### G. 小白的烦恼（分块 + 莫队）

- **知识点**：莫队算法、分块、根号分治
- **难度**：★★★★☆
- **AC 时间**：14:52，3 次错误（TLE×2/WA）

**分析**：

本题要求对每个询问 (l, r, k)，找到最小的 d 使得 f(d, l, r) ≤ k。

代码使用了根号分治 + 莫队的思路：
1. 将天数分为"大天数"（出现次数 > B）和"小天数"
2. 大天数直接预处理前缀和，对每个询问 O(1) 判断
3. 小天数用莫队维护，每个天数维护一个"当前值"的桶

**错误分析**：

1. **第一次 TLE**（14:37）：`isbig` 数组初始化错误，导致所有天数都被当作大天数处理，复杂度退化为 O(m × q)。
2. **第二次 TLE**（14:42）：`isbig` 全部设为 1，同样导致复杂度退化。
3. **第一次 WA**（14:51）：k 的范围处理不当。当 k < -B 时，`D[k+B]` 会数组越界；当 k > B 时，应该截断到 B。

修复后的关键代码：

```cpp
if(k<-B) continue;
k=min(k, B);
```

**评价**：这是一道典型的"根号分治 + 莫队"综合题，难度较高。队伍经历了三次错误才 AC，主要问题在于：
- 对 `isbig` 的初始化逻辑不够清晰
- 对 k 的边界处理不够仔细

建议在实现根号分治时，先明确"大"和"小"的划分标准，再仔细处理边界条件。

---

### E. 大户爱的生成树2（全局最小割 + MST）

- **知识点**：Stoer-Wagner 算法、最小生成树、割树（Gomory-Hu Tree）
- **难度**：★★★★★
- **首次提交即 AC**，用时 155 分钟

**分析**：

本题要求构造完全图 B（边权为原图任意两点间的最小割），求 B 的最小生成树边权之和。

代码使用了 Stoer-Wagner 算法计算全局最小割，然后利用 Gomory-Hu Tree 的性质：任意两点间的最小割等于它们在割树上路径的最小边权。最后对割树求 MST。

```cpp
int contract(int &s,int &t){
    // Stoer-Wagner 的 contract 操作
    // ...
}

int solve(){
    for(int t=1;t<n;t++){
        int res=contract(s,t);
        bin[t]=1;
        tot=min(tot,res);
        // 合并 s 和 t
    }
    return tot;
}
```

**评价**：这是一道高难度的综合题，需要同时掌握 Stoer-Wagner 算法和 Gomory-Hu Tree 的性质。队伍在 155 分钟内 AC，展现了扎实的图论功底。代码实现简洁高效，直接使用 Stoer-Wagner 求全局最小割，然后利用割树性质求 MST。

---

### I. 大改革（贪心构造）

- **知识点**：贪心、分类讨论、构造
- **难度**：★★★★☆
- **AC 时间**：16:07，1 次 WA + 6 次 CE

**分析**：

本题要求安排操作顺序，最大化两派系提供的动力总和。

代码将操作分为四类：
- cnt[0]：鹰派期望 ≠ t，鸽派期望 = t（鸽派满意，鹰派不满意）
- cnt[1]：鹰派期望 = t，鸽派期望 ≠ t（鹰派满意，鸽派不满意）
- cnt[2]：两派期望都 = t（两派都满意）
- cnt[3]：两派期望都 ≠ t（两派都不满意）

贪心策略：
1. 先执行 cnt[2]（两派都满意），提升满意度
2. 交替执行 cnt[0] 和 cnt[1]，利用满意度阈值 c 获得额外动力
3. 最后执行 cnt[3]（两派都不满意）

**错误分析**：

第一次 WA 的原因是在执行 cnt[0] 时没有检查 `cnt[0] > 0`：

```cpp
for(int i=1;i<=c-s+1;i++){
    proc(0,1);proc(1,-1);
    cnt[0]--;
    if(cnt[0] == 0)break;  // 这里 break 了，但循环条件没有检查 cnt[0]
}
```

修复后增加了 `if(cnt[0] <= 0) break;` 的判断。

**关于 CE**：从 16:20 到 16:45 的 6 次 CE 提交内容均为无关的调试输出（如组合数表、二进制串等），很可能是队员将提交当作"虚拟打印"使用。虽然不计罚时，但反映了代码管理上的混乱。

**评价**：这是一道需要仔细分类讨论的贪心题。队伍在 40 分钟内解决了这个问题（15:27 首次提交到 16:07 AC），但中间的 CE 提交浪费了一些时间。

---

### A. 串串（未通过）

- **知识点**：后缀自动机（SAM）、后缀数组、字符串匹配
- **难度**：★★★★★
- **状态**：未通过

**分析**：

本题要求统计 k-优秀子串的数量。k-优秀子串定义为可以分解为 k 个连续段，每段长度递增 1，且每段是前一段的"扩展"（即前一段是后一段的前缀）。

**可能的解题思路**：
1. 使用后缀自动机（SAM）或后缀数组预处理所有子串的 LCP 信息
2. 对于每个起始位置，使用倍增/二分找到最长的 k-优秀子串
3. 利用 SAM 的 parent 树和 fail 指针优化匹配过程

**未通过原因**：本题难度较高，需要综合运用字符串高级算法。队伍在比赛后期将精力集中在 E 和 I 题上，没有足够时间深入思考 A 题。

---

### L. 飞翔的甲鱼（未通过）

- **知识点**：博弈论、SG 函数、Nim 游戏变种
- **难度**：★★★★★
- **状态**：未通过

**分析**：

本题是一个组合博弈问题。甲鱼可以飞到任意更小的空格子，两人轮流操作。

**可能的解题思路**：
1. 将每个甲鱼看作一个 Nim 堆，大小为它前面空格的数量
2. 计算 SG 函数，判断异或和是否为 0
3. 由于 n 可达 10⁵，需要 O(n) 或 O(n log n) 的算法

**未通过原因**：本题的博弈模型较为隐蔽，需要深入分析 SG 函数的性质。队伍在比赛后期没有足够时间处理此题。

---

## 三、补题推荐

### 推荐补题：A. 串串

**推荐理由**：
- 这是一道高质量的字符串综合题，涉及后缀自动机/后缀数组、LCP 查询、倍增等多项技术
- 题目设计巧妙，将"k-优秀"的定义与字符串匹配紧密结合
- 对提升字符串算法的综合运用能力有很大帮助

**建议**：
- 先学习后缀自动机（SAM）的基本原理和构建方法
- 理解 SAM 的 parent 树和 fail 指针在 LCP 查询中的应用
- 尝试用 SAM + 倍增的方法解决此题
- 对比后缀数组的解法，理解两种方法的优劣

### 推荐补题：L. 飞翔的甲鱼

**推荐理由**：
- 这是一道有深度的博弈论题目，需要将问题转化为 Nim 游戏
- 考察对 SG 函数的理解和灵活运用
- 数据范围较大（T ≤ 10⁷），对算法效率要求高

**建议**：
- 先复习 Nim 游戏和 SG 函数的基本理论
- 分析甲鱼移动的 SG 值变化规律
- 尝试将问题转化为"每个甲鱼前面空格数"的异或和
- 注意输入数据量极大，需要使用高效的输入方式

---

## 四、失误与改进建议

### 4.1 具体失误点

| 题目 | 失误类型 | 具体问题 | 影响 |
|------|---------|---------|------|
| F | 低级笔误 | 线段树合并操作中 `z.c[0]` 被赋值两次 | 1 次 RE，20 分钟罚时 |
| J | 调试残留 | 提交时未删除调试输出语句 | 1 次 RE，20 分钟罚时 |
| J | 逻辑错误 | 树状数组 `change` 应为 `max` 而非赋值 | 1 次 TLE，20 分钟罚时 |
| G | 初始化错误 | `isbig` 数组初始化逻辑错误 | 2 次 TLE，40 分钟罚时 |
| G | 边界处理 | k 的范围未正确处理 | 1 次 WA，20 分钟罚时 |
| I | 边界检查 | 循环中未检查 `cnt[0] > 0` | 1 次 WA，20 分钟罚时 |
| I | 代码管理 | 多次 CE（疑似虚拟打印） | 浪费时间，影响节奏 |

### 4.2 改进建议

**技术层面：**

1. **代码审查习惯**：在提交前花 1-2 分钟检查代码，特别是：
   - 是否有调试输出残留
   - 数组大小是否足够
   - 边界条件是否处理完整
   - 赋值操作是否有重复或遗漏

2. **模板规范化**：建立统一的代码模板，特别是线段树、树状数组、NTT 等常用数据结构，减少笔误概率。

3. **复杂度预估**：在实现算法前，先估算时间复杂度和空间复杂度，避免 TLE 和 MLE。特别是分块、莫队等算法，要注意常数优化。

4. **边界条件测试**：在提交前，用样例和小数据测试边界情况，如 k = 0、k = 最大值、空区间等。

**策略层面：**

1. **题目选择**：建议按照"签到题 → 简单题 → 中等题 → 难题"的顺序推进。本场比赛中 K、H、B 三题在 30 分钟内解决，节奏很好。

2. **时间分配**：对于一道题，如果 30 分钟内没有明确思路，建议先跳过做其他题，避免在一道题上花费过多时间。本场比赛中 A 题和 L 题在后期没有足够时间，说明前期在难题上花费时间过多。

3. **团队协作**：
   - 一人负责读题和思考，一人负责编码，一人负责审查代码
   - 在提交前，由非编码队员审查代码，减少低级错误
   - 使用"虚拟打印"功能时注意标记，避免混淆

4. **罚时控制**：本场比赛因错误提交产生了约 140 分钟的额外罚时。建议在不确定答案时，先通过样例和小数据测试，再提交。

### 4.3 结语

本场比赛队伍展现了较强的综合实力，10/12 题的通过率在 ACM 比赛中属于优秀水平。开局节奏好、中期稳定、后期有产出，整体表现值得肯定。

但同时也暴露出一些需要改进的地方：低级错误导致的罚时、代码审查不够仔细、边界条件处理不当等。如果能在这些方面加以改进，队伍的竞争力将进一步提升。

**给队员的鼓励**：你们在 E 题（全局最小割）和 I 题（贪心构造）上的表现非常出色，展现了扎实的算法功底和良好的问题分析能力。继续保持这种势头，在后续的训练中注重细节、减少失误，相信你们能在正式比赛中取得更好的成绩！加油！💪